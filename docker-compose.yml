services:
  hela:
    image: ghcr.io/l8tenever/hela:latest
    container_name: hela
    restart: unless-stopped
    ports:
      - "8000:8000"
      - "8080:8080"
    volumes:
      - hela-data:/app/data

volumes:
  hela-data:
